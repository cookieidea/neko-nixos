## MPV总配置：
 
 # 说明可参考：
 # https://github.com/dyphire/mpv-config/blob/master/mpv.conf
 # https://github.com/hooke007/mpv_PlayKit/blob/main/portable_config/mpv.conf

 idle=yes
 osd-bar=no
 scale=lanczos
 dscale=lanczos
 volume-max=100
 volume=80
 sub-auto=fuzzy
 keepaspect-window=no
 hr-seek-framedrop=no
 directory-mode=ignore
 autocreate-playlist=same
 image-subs-hdr-peak=video
 input-builtin-bindings=no
 slang=sc,chs,zh-cn,zh-hans,tc,cht,zh-hant,zh-hk,zh-mo,zh-tw
# Linux（AMD）硬件解码（原 dotfiles/config/mpv/config 并入）
hwdec=auto-safe
# B 站大会员清晰度：ytdl_hook 传 cookies 给 yt-dlp（文件不入 git，权限 600）
ytdl-raw-options-append=cookies=/home/cookie/.config/bilibili.cookies
